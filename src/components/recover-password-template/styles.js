import styled from "styled-components";

export const RecoverContainer = styled.form`
    display: flex;
    flex-direction: column;
    justify-content: center;
    gap: 1rem;


`

export const ControlRecoverContainer = styled.div`
    display: flex;
    align-self: flex-end;
    gap: 1rem;
`
