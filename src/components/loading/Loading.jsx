import React from 'react'
import { Loader, LoaderContainer } from './styles'

const Loading = () => {
  return (
    <LoaderContainer>
        <Loader></Loader>
    </LoaderContainer>
  )
}

export default Loading